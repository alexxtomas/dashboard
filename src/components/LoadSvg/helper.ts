import { SHARED_ERROR_MESSAGE } from '@components/shared/helper.ts'

const { SHADOW_ROOT_NOT_FOUND } = SHARED_ERROR_MESSAGE

export const ERROR_MESSAGE = {
  SHADOW_ROOT_NOT_FOUND,
  SRC_REQUIRED: 'src attribute is required'
}
